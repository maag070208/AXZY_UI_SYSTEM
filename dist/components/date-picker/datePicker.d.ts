import { ITDatePickerProps } from './date-picker.props';
export default function ITDatePicker({ name, value, onChange, onBlur, variant, size, className, calendarClassName, disabled, label, touched, error, required, placeholder, minDate, maxDate, }: ITDatePickerProps): import("react/jsx-runtime").JSX.Element;
