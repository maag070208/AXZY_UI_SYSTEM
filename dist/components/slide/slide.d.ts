import { ITSlideToggleProps } from './slide.props';
/**
 * Slide toggle switch component.
 * Supports fully controlled (`isOn`) or uncontrolled (`initialState`) modes.
 */
export default function ITSlideToggle({ onToggle, isOn: controlledIsOn, initialState, activeColor, inactiveColor, // default gray-400
disabled, size, className, }: ITSlideToggleProps): import("react/jsx-runtime").JSX.Element;
