import { ITCardProps } from './card.props';
/**
 * Componente de tarjeta (Card) personalizable.
 */
export default function ITCard({ title, image, alt, children, actions, className, imageClassName, titleClassName, contentClassName, actionClassName, onClick, }: ITCardProps): import("react/jsx-runtime").JSX.Element;
