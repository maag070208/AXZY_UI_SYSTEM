interface PaginationInfoProps {
    currentCount: number;
    totalCount: number;
    className?: string;
}
export default function PaginationInfo({ currentCount, totalCount, className, }: PaginationInfoProps): import("react/jsx-runtime").JSX.Element;
export {};
