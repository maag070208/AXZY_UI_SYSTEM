import { LoaderProps } from './loader.props';
export default function ITLoader({ size, variant, color, // Default to semantic primary
className, }: LoaderProps): import("react/jsx-runtime").JSX.Element;
